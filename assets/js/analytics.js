/* ============================================================
   ANALYTICS — Google Analytics 4
   Replace GA_MEASUREMENT_ID with your actual ID from
   Google Analytics → Admin → Data Streams → Measurement ID
   ============================================================ */

// Uncomment and replace with your real Measurement ID after setup:
//
// window.dataLayer = window.dataLayer || [];
// function gtag(){ dataLayer.push(arguments); }
// gtag('js', new Date());
// gtag('config', 'GA_MEASUREMENT_ID');
//
// Also add this in the <head> of every page:
// <script async src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"></script>

